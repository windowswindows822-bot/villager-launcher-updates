
<Page
    x:Class="villagerlauncher.Pages.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="30" Spacing="16">
            <StackPanel>
                <TextBlock Text="Tools &amp; Diagnostics" FontSize="32" FontWeight="SemiBold"/>
                <TextBlock Text="Recovery and troubleshooting get a dedicated workspace."
                           Opacity="0.6"/>
            </StackPanel>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Padding="20" CornerRadius="16" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Diagnostics" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="Scan Java, Minecraft files, settings, assets, and launcher state."
                                   TextWrapping="Wrap"/>
                        <Button Content="Run Scan" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Padding="20" CornerRadius="16" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Repair All" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="Back up state first, repair safe issues, verify, then report."
                                   TextWrapping="Wrap"/>
                        <Button Content="Repair All" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Padding="20" CornerRadius="16" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Logs" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="Export plain-text logs that are readable by you or GPT."
                                   TextWrapping="Wrap"/>
                        <Button Content="Export Log" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Padding="22" CornerRadius="16" BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Diagnostic Timeline" FontSize="19" FontWeight="SemiBold"/>
                    <TextBlock Text="No scan has run in this redesigned session."
                               Opacity="0.65"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
