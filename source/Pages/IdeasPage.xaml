
<Page
    x:Class="villagerlauncher.Pages.IdeasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="30" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Ideas Lab"
                           FontSize="32"
                           FontWeight="SemiBold"/>
                <TextBlock Text="500 redesign ideas — searchable, filterable, and ready to turn into real features."
                           Opacity="0.6"/>
            </StackPanel>
            <Border Grid.Column="1"
                    Padding="16,8"
                    CornerRadius="20"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    VerticalAlignment="Center">
                <TextBlock x:Name="IdeaCountText"
                           Text="500 ideas"
                           FontWeight="SemiBold"/>
            </Border>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox"
                     PlaceholderText="Search the 500 ideas…"
                     TextChanged="SearchBox_TextChanged"/>
            <ComboBox x:Name="CategoryCombo"
                      Grid.Column="1"
                      SelectionChanged="CategoryCombo_SelectionChanged"/>
        </Grid>

        <InfoBar Grid.Row="2"
                 IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Title="Design backlog"
                 Message="These are candidates for the redesign. They are not all implemented yet."/>

        <ListView x:Name="IdeasList"
                  Grid.Row="3"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8"
                            Padding="16"
                            CornerRadius="12"
                            BorderThickness="1"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <Grid ColumnSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="58"/>
                                <ColumnDefinition Width="190"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding NumberText}"
                                       FontWeight="SemiBold"
                                       Opacity="0.55"/>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding Category}"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>

                            <StackPanel Grid.Column="2" Spacing="4">
                                <TextBlock Text="{Binding Title}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}"
                                           Opacity="0.65"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
