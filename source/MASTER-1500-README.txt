VILLAGER LAUNCHER 2.2.2 — VERIFIED 1500+ MASTER POOL INSTALL

Source:
The user's uploaded 1500+ master idea pool.

Catalog:
1,501 numbered records are preserved in source order.
The source itself contains source-number 101 twice, so this installer keeps a separate Master ID (1-1501) and the original Source Number.

Install batches:
Batch 1: Master IDs 1-500
Batch 2: Master IDs 501-1000
Batch 3: Master IDs 1001-1501

Hidden rules:
The 100 Easter Egg / hidden-feature records are stored in Batch 3 but filtered from the normal Master Feature Pool UI.
Villager OS is not added to the main navigation, News, What's New, or release notes.

Safety:
- MainWindow uses true Frame navigation.
- SelectionChanged is attached only after InitializeComponent().
- App.xaml restores XamlControlsResources.
- Each script validates JSON counts.
- Batch 1 validates generated XAML and the patched .csproj as XML.
- Every batch rebuilds x64 / win-x64.
- Every batch confirms its JSON files were copied into the build output.

Important:
Presence in the Master Feature Pool is a backlog/catalog record, not a false claim that every complex launcher feature is already functionally implemented.
